---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: traefik
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: flux-system
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2beta1
      kind: HelmRelease
      name: traefik
  interval: 5m0s
  path: ./kubernetes/infrastructure/base/traefik
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
